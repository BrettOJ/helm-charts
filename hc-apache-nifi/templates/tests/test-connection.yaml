apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hc-apache-nifi.fullname" . }}-test-connection"
  labels:
    {{- include "hc-apache-nifi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hc-apache-nifi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
